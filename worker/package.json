{
  "name": "schemap-license-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker Serverless License API for Schemap",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:init": "wrangler d1 execute schemap-licenses-db --local --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240304.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.35.0"
  }
}

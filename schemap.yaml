# Schemap Configuration Asset
database:
  connection_url: "sqlite:///demo_ecommerce.db"
  exclude_tables:
    - "spatial_ref_sys"
    - "alembic_version"
output:
  file_path: "./schemap_database_context.md"
  format: "markdown"

domain:
  mappings:
    cust: "Customer"
    tx: "Transaction"
    inv: "Invoice"
    acct: "Account"
